create schema test_db;
set search_path=test_db;

CREATE TABLE tests (
    id SERIAL PRIMARY KEY,
    valeur NUMERIC(10, 2)
);

INSERT INTO tests (valeur) VALUES (125.8);
INSERT INTO tests (valeur) VALUES (125.85);

-- Test 1: Using the 'Cool' mask (9) for decimals
SELECT to_char(valeur, '000 999.99') as resultat_cool 
FROM tests;

-- Test 2: Using the 'Strict' mask (0) for decimals
SELECT to_char(valeur, '000 999.00') as resultat_strict 
FROM tests;